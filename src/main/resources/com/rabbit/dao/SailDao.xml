<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbit.dao.SailDao">
    <insert id="addSail">
        insert into sale(gid, nums) values (#{gid}, #{nums})
    </insert>
</mapper>